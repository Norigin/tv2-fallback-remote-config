# tv2-fallback-remote-config
Remote config for TV2 app

# Config.json file
It consist on an object where keys are "Parameter key" (firebase) and the values are the same as the "Default value" (firebase).
